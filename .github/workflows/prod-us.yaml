name: Deploy to Prod - US Region

on:
  pull_request:
    branches:
      - prod

jobs:
  deploy-prod-us:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Deploy to Prod - US
        run: |
          echo "Deploying to Prod US Region with Image Tag: ${{ secrets.PROD_US_IMAGE_TAG }}"
