name: Prod - AP Region

on:
  pull_request:
    types: [opened, edited, reopened]
    paths:
      - .github/workflows/prod-ap.yaml

jobs:
  deploy-prod-ap:
    if: >
      github.event.pull_request.title contains 'ap' &&
      github.event.pull_request.title contains 'eu' &&
      github.event.pull_request.title contains 'us' &&
      github.event.pull_request.title contains 'rG'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Validate Prod AP Deployment
        run: |
          PR_BODY=$(jq -r '.pull_request.body' < "$GITHUB_EVENT_PATH")
          PROD_SELECTED=$(echo "$PR_BODY" | grep -oP '(?<=- Prod: `)[^`]+')

          if [ "$PROD_SELECTED" != "Y" ]; then
            echo "Prod environment not selected for deployment. Skipping."
            exit 0
          fi

          PROD_IMAGE_AP=$(echo "$PR_BODY" | grep -A 3 "Prod Environment" | grep "AP Region:" | awk -F'`' '{print $2}' | tr -d '[:space:]')
          if [ -z "$PROD_IMAGE_AP" ]; then
            echo "Error: Missing image tag for Prod AP Region."
            exit 1
          fi

          echo "Deploying Prod - AP Region with image tag: $PROD_IMAGE_AP"
