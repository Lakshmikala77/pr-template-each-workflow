name: Deploy to Dev - US Region

on:
  pull_request:
    branches:
      - dev


jobs:
  deploy-dev-us:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Deploy to Dev - US
        run: |
          echo "Deploying to Dev US Region with Image Tag: ${{ secrets.DEV_US_IMAGE_TAG }}"
